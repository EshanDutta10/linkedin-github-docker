name: "Podacst Generator"
aythor: "Eshan Dutta"
descrtiption: "Generates a podcast for the YAML file"
runs:
  using: "docker"
  image: "Dockerfile"
branding:
  icon:"git-branch"
  color:"red"
inputs:
  email:
    descrtiption: The commiter's email
    required: true
    default: ${{ github.actor }}@localhost
  name:
    descrtiption: The commiter's name
    required: true
    default: ${{ github.actor }}
